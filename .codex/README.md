# Codex

Codex 协作规则以仓库根目录 `AGENTS.md` 为准。

当前阶段仅允许项目初始化和 `clean_diffusion/ddpm.py` 单文件 DDPM baseline，不要直接实现 FM / CFM / DMD。

本项目用于教学学习，优先 CleanRL-style 端到端单文件算法，不要默认创建深层包结构。

单文件内要突出算法相关代码和端到端流程，阅读顺序必须连贯。
